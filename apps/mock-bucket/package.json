{
  "name": "@apps/mock-bucket",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run create-data && docker compose up -d && bun run import-data",
    "start": "bun run create-data && docker compose up -d && bun run import-data",
    "serve": "bun run create-data && docker compose up -d && bun run import-data",
    "create-data": "bun run src/create-data.ts",
    "import-data": "bun run src/import-data.ts"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.28.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "config": "workspace:*"
  }
}
